directive @adminAuth(token: String) on FIELD_DEFINITION | ARGUMENT_DEFINITION | INPUT_FIELD_DEFINITION

type Query {
    me: [User] @all
}

extend type Query{
    adminToken(token: String!): [User!]! @adminAuth @all
}





type User{
    id: ID!
    address: String

}
